{
  "name": "HelloWorld",
  "version": "1.0.0",
  "description": "The SubQuery Exercise 1",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Yaroslav_Chikal",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^6",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "vim": "^0.1.0"
  }
}
